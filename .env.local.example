# Server-only — none of these are NEXT_PUBLIC. The browser never sees them.
ANTHROPIC_API_KEY=
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Optional access gate. Comma-separated; "@domain" entries match the whole
# domain, anything else matches exactly. Defaults to the value below.
ALLOWED_EMAILS=connor.brewer@icloud.com,@kpmg.ca
